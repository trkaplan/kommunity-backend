build:
  docker:
    web: Dockerfile
    worker: Dockerfile
run:
  web: "npm run start:pipeline"
setup:
  addons:
    - plan: heroku-postgresql
release:
  image: worker
  command:
    - node ./scripts/db/staging-db-setup.js
