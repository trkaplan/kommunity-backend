build:
  docker:
    web: Dockerfile
    worker: Dockerfile
run:
  web: "npm run start:pipeline"
setup:
  addons:
    - plan: heroku-postgresql
release:
  image: worker
  command:
    - "ls -l /usr/app && ls -l /usr/app/db && ls -l /usr/app/db/scripts"
    - node ./scripts/db/staging-db-setup.js
