build:
  docker:
    web: Dockerfile
    worker: Dockerfile.worker
run:
  web: "npm run start:pipeline"
setup:
  addons:
    - plan: heroku-postgresql
release:
  image: worker
  command:
    - echo HI
